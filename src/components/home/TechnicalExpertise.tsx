import { Button } from "@/components/ui/button";
import { expertiseCategories } from "@/data/expertise";
import { ExpertiseCategoryCard } from "./ExpertiseCategoryCard";
import { AnimatedSection } from "@/components/AnimatedSection";
interface TechnicalExpertiseProps {
  onBookDemo: () => void;
}
export const TechnicalExpertise = ({
  onBookDemo
}: TechnicalExpertiseProps) => {
  return <section id="teknologi" className="relative py-24 overflow-hidden bg-gradient-to-b from-background via-primary/5 to-background">
      {/* Animated background elements */}
      
      
      
      
    </section>;
};